"""Forensics -- read-only statistical and structural analysis of bytes.

The signatureless surface: anomaly checks (anomalies), statistical audio
detection (audioscan, framescan), carving/recovery orchestration (locate),
steganography and transform lenses (lsb, transforms), structural triage of
unknown containers (triage), container-vs-claim integrity (integrity), tool-chain
provenance (provenance), and terminal byte visualization (viz). Nothing here
mutates a file; it only measures and reports.
"""
