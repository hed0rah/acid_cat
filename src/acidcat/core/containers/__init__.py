"""Disc-image and filesystem containers.

Optical-disc and cartridge images that hold audio as files or streams: GameCube
GCM/ISO (gcm), Wii partitioned discs (wiidisc), ISO 9660 filesystems (iso9660),
and CD cue sheets (cue). These locate and yield the embedded byte ranges; the
codecs decode them.
"""
